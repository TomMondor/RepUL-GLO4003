package ca.ulaval.glo4003.repul.commons.domain.uid;

import java.util.UUID;

public class SubscriberUniqueIdentifier extends UniqueIdentifier {
    public SubscriberUniqueIdentifier(UUID uuid) {
        super(uuid);
    }
}
